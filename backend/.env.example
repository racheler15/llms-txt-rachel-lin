ANTHROPIC_API_KEY=your-api-key-here

# Optional: SQLite scan persistence (default: backend/data/scans.db)
# SCAN_DB_PATH=backend/data/scans.db

# Optional: background rescan scheduler (default: enabled)
# SCAN_SCHEDULER_ENABLED=true
# SCAN_INTERVAL_HOURS=24
# SCAN_SCHEDULER_TICK_SECONDS=900
